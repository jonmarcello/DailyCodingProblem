/**
 * Given a string consisting of parentheses, single digits,
 * and positive and negative signs, convert the string into a
 * mathematical expression to obtain the answer.
 * 
 * @param {*} str 
 */
function parse(str) {

}